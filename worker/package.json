{
  "name": "cv-tailor-worker",
  "private": true,
  "version": "1.0.0",
  "description": "Cloudflare Worker: holds the OpenAI key server-side, proxies job-offer fetching and CV tailoring for the static web app.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "typescript": "^6.0.2",
    "wrangler": "^4.20.0"
  }
}
