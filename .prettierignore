node_modules
dist
coverage
output
package-lock.json

# Local tool state, not part of the project
.claude

# shadcn-generated components: vendored code, regenerated by
# `shadcn add --overwrite`, and never hand-edited. Reformatting these
# to the house style would just fight the generator on every update.
web/src/components/ui/
